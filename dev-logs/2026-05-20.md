# 开发日志 — 2026-05-20

## 完成事项

- [x] 修复分类名称/颜色编辑后不刷新 → @Builder 改为 ForEach 内联渲染
- [x] 将所有 ViewModel 写操作统一改为**乐观更新模式**（先更新 UI 再写 DB）
  - CategoryViewModel: addCategory / updateCategory / deleteCategory
  - ClipboardViewModel: moveToCategory / deleteClip（togglePin 此前已改）
- [x] 修复后台剪贴板静默记录（第二次修正）
  - 需求澄清：后台运行也算程序在运行，应继续监听；只有 App 进程完全关闭时不应记录
  - 根因：`aboutToDisappear` 中 `release()` 在退到后台时关闭了监听；且启动时无基线机制
  - 修复：
    1. 移除 `aboutToDisappear → release()` — 后台保持监听
    2. 移除 `isPaused` 机制（方向反了）
    3. 新增 `needsBaseline` 标志：启动后首次读到剪贴板只建基线（记 `lastContent`），不存 DB
    4. `establishBaseline()` 显式读一次剪贴板建立基线
    5. 后续只保存与基线不同的内容
- [x] 修复点击复制时不重复存入 → `copyToClipboard` 增设 `lastContent`
  - 问题：`skipNextUpdate` 只防事件路径，轮询每 2.5s 仍会读到同一内容并保存
  - 修复：`copyToClipboard` 中设 `lastContent = clip.content`，`saveClip` 的去重逻辑同时防护事件和轮询两条路径
- [x] 显示每条内容的精准存入时间 → `formatTime` 去掉相对时间，统一显示 MM-DD HH:mm
- [x] 将 ArkTS @Builder + ForEach key 的陷阱记入 CLAUDE.md
- [x] 归档 2026-05-19 日志

## 待办事项

- [ ] 编译验证 → 编辑分类名称/颜色 → 确认列表即时更新
- [ ] 图片剪贴板保存和显示
- [ ] 搜索/分类筛选与 fullClips 架构的联动验证
- [ ] 真机测试

## 遇到的问题

1. 分类名称/颜色编辑后，列表页不刷新，需要重启 App 才显示
   - 根因：ArkTS V2 中 ForEach + key 函数复用 DOM 节点时，`@Builder` 函数不会重新执行
   - 解决：将行内容从 `@Builder buildCategoryRow(cat)` 改为直接内联在 ForEach 回调中
   - 模式对比：
     ```
     // ❌ ForEach + key 复用时，@Builder 不重新执行
     ForEach(arr, item => this.buildRow(item), key: item => item.id)
     
     // ✅ 内联渲染，ForEach 每次迭代都执行最新代码
     ForEach(arr, item => { /* inline Row() { ... } */ }, key: item => item.id)
     ```
